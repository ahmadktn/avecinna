# Dual PostgreSQL Database Connection Strings (Docker Compose Setup)
DATABASE_URL_PRIMARY=postgres://postgres:postgresPassword123!@localhost:5432/avecinna_primary_db
DATABASE_URL_AUDIT=postgres://audit_admin:auditPassword123!@localhost:5433/avecinna_audit_db

# Fastify Server Configuration
PORT=4000
HOST=0.0.0.0
NODE_ENV=development

# Authentication Secrets
JWT_SECRET=avecinna_super_secret_jwt_key_icsc_2026_hackathon
