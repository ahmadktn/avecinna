---
title: Mode B: Zero-Trust Reverse Proxy Sidecar
description: Securing existing hospital EMRs (OpenMRS, Bahmni, FHIR servers) without modifying upstream code through a zero-trust gateway sidecar.
---

# Mode B: Zero-Trust Reverse Proxy Sidecar

Hospitals often run legacy or third-party EMR systems (e.g., OpenMRS, Bahmni, Epic, or standard HL7 FHIR servers) that lack context-aware authorization, server-side DTO masking, or immutable audit ledgers.

**Mode B** deploys Avecinna as a **Security Reverse Proxy Gateway Sidecar** (`backend/src/routes/proxy.ts`), intercepting all upstream clinical traffic without requiring code modifications to the legacy EMR.

---

## 1. Proxy Architecture & Interception Flow

```mermaid
sequenceDiagram
    autonumber
    actor Client as Clinical Workstation
    participant Proxy as Avecinna Mode B Proxy Gateway (:3001)
    participant CAAC as CAAC Engine
    participant Masker as Server-Side DTO Masker
    participant ADB as avecinna_audit_db (Isolated)
    participant Legacy as Legacy EMR (OpenMRS / FHIR :8080)

    Client->>Proxy: GET /api/v1/emr-proxy/Patient/p-cardio-01
    Note over Proxy: Authenticate JWT & Resolve Active Ward
    Proxy->>CAAC: evaluateCAAC(user, "p-cardio-01")

    alt CAAC Denied (BOLA Violation / Expired Shift)
        CAAC-->>Proxy: Denied (403 Forbidden)
        Proxy->>ADB: Append PROXY_CAAC_BLOCK
        Proxy-->>Client: 403 Forbidden (Blocked before touching upstream)
    else CAAC Permitted
        CAAC-->>Proxy: Permitted
        Proxy->>Legacy: GET /ws/rest/v1/patient/p-cardio-01
        Legacy-->>Proxy: 200 OK (Raw Upstream Record)
        Proxy->>Masker: filterPatientRecordByRole(rawRecord, role)
        Proxy->>ADB: Append PROXY_CAAC_PERMIT (SHA-256 Payload Hash)
        Proxy-->>Client: 200 OK (Masked DTO + X-Avecinna Headers)
    end
```

---

## 2. Configuration & Route Mapping

Enable Mode B in `backend/.env`:

```ini
UPSTREAM_EMR_URL=https://openmrs.hospital.internal/ws/rest/v1
```

All incoming calls matching `/api/v1/emr-proxy/*` are dynamically routed to `UPSTREAM_EMR_URL/*` with:
- Upstream credentials / service tokens attached.
- Inbound headers (`X-User-Id`, `X-Active-Ward-Id`, `X-User-Role`) validated.
- Response payloads scrubbed through `filterPatientRecordByRole`.
- Outbound headers attached:
  - `X-Avecinna-Execution-Mode: MODE_B`
  - `X-Avecinna-Audit-Logged: true`
  - `X-Audit-Block-Hash: <sha256>`
